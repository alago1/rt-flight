syntax = "proto3";

package messaging;

service MessagingService {
    rpc GetBoundingBoxes (File_Payload) returns (BBoxes) {}
}

message File_Payload {
    string path = 1;
    string jsondata = 2;
}

message BBoxes {
    string jsondata = 1;
}